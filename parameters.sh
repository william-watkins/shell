#!/bin/bash

echo "script initialized with $# parameters."
echo "first parameter is $1"
# $# display the numbers of parameters entered at the script call
# $1, $2, ... call the specified parameter in the script 
